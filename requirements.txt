# Lambda Cut Requirements
# Python dependencies for Lambda Cut - YouTube Shorts Pipeline

# Core
yt-dlp>=2024.1.0

# Transcription (faster-whisper is primary, with fallbacks)
faster-whisper>=1.0.0
stable-ts>=2.0.0

# Security: System keychain for API keys
keyring>=23.0.0

# Optional: For better GPU acceleration
# torch>=2.0.0
# torchaudio>=2.0.0
