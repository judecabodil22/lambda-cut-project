name: Lint

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    runs-on: ubuntu-latest
    
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install flake8
    
    - name: Lint with flake8
      run: |
        flake8 workflows/ --max-line-length=120 --ignore=E501,W503,E221,E222,E302,E305,E401,E701,E702,E731,E741,E722,W293,E128,E127,E121,E231,E306
    
    - name: Check for common issues
      run: |
        # Check for debug statements left in code
        grep -r "print(" workflows/*.py || true
        grep -r "TODO" workflows/*.py || true
        grep -r "FIXME" workflows/*.py || true
